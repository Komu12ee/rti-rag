name: Deploy RTI-RAG

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy to VM
        run: |
            /home/cmit/project/deploy-rti-rag.sh